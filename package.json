{
  "name": "vivanteplexus-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Robotimize's offline-first multilingual rehabilitation dose, equipment and outcome intelligence prototype",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check i18n.js && node --check app-v4.js && node --check equipment-upgrade.js"
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  },
  "license": "MIT"
}
